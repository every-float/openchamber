## 1. 前端技术栈
核心框架和工具：

- React 19.1.1 - 使用 TypeScript ( .tsx 文件)
- Vite 7.1.2 - 构建工具和开发服务器
- TypeScript 5.8.3 - 类型系统
- Bun - 包管理器 (版本 1.3.5)
UI 和样式：

- Tailwind CSS 4.0 - CSS 框架
- Radix UI - 无样式 UI 组件库（dialog、dropdown-menu、select、tooltip 等）
- Motion (Framer Motion) - 动画库
- next-themes - 主题管理
- class-variance-authority + clsx + tailwind-merge - 样式工具
状态管理：

- Zustand 5.0.8 - 状态管理库
代码编辑器：

- CodeMirror 6 - 代码编辑器（支持多种语言语法高亮）
其他重要依赖：

- react-markdown + remark-gfm - Markdown 渲染
- react-syntax-highlighter - 代码高亮
- @opencode-ai/sdk - OpenCode AI SDK
- @dnd-kit - 拖拽功能
- sonner - Toast 通知
- cmdk - 命令面板
## 2. 前端代码位置
这是一个 monorepo 结构，前端代码分布在以下位置：

包名 路径 说明 @openchamber/ui /home/szy/openchamber/packages/ui/src/ 核心 UI 组件库 @openchamber/web /home/szy/openchamber/packages/web/src/ Web 应用入口 @openchamber/desktop /home/szy/openchamber/packages/desktop/ Tauri 桌面应用 @openchamber/vscode /home/szy/openchamber/packages/vscode/ VS Code 扩展

主要目录结构：

```
packages/ui/src/
├── components/          # 组件目录
│   ├── chat/           # 聊天相关组件
│   ├── layout/         # 布局组件
│   ├── session/        # 会话管理组件
│   ├── sections/       # 设置页面各部分
│   ├── ui/             # 基础 UI 组件
│   ├── views/          # 视图组件
│   ├── providers/      # Provider 组件
│   └── ...
├── stores/             # Zustand 状态管理
├── hooks/              # React Hooks
├── contexts/           # React Context
├── lib/                # 工具库
└── styles/             # 样式文件
```
## 3. 国际化配置
当前状态：没有国际化支持

经过搜索，项目目前 没有 任何国际化相关的配置或库：

- 未安装 i18next 、 react-intl 、 formatjs 等国际化库
- 没有 i18n/ 、 locales/ 、 lang/ 、 translations/ 等目录
- 所有 UI 文本都是 硬编码 在组件中（英文）
- package.json 中没有任何 i18n 相关依赖
示例（硬编码文本）：

```
// packages/ui/src/components/layout/MainLayout.tsx
aria-label="Close drawer"
```
## 4. 前端入口文件和主要组件结构
### 入口文件
UI 包入口 - main.tsx ：

```
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import { SessionAuthGate } from './components/auth/
SessionAuthGate'
import { ThemeSystemProvider } from './contexts/
ThemeSystemContext'
// ...
```
Web 应用入口 - main.tsx ：

```
import { createWebAPIs } from './api';
import { registerSW } from 'virtual:pwa-register';
// ...
import('@openchamber/ui/main');
```
### 主要组件结构
App 组件 - App.tsx ：

- 初始化应用状态
- 根据运行时环境渲染不同布局
- 支持三种运行时：Desktop、Web、VS Code
主要布局组件：

- MainLayout.tsx - 桌面端主布局
- VSCodeLayout.tsx - VS Code 扩展布局
主要视图组件：

- ChatView - 聊天视图
- GitView - Git 操作视图
- TerminalView - 终端视图
- FilesView - 文件浏览视图
- SettingsView - 设置视图
- PlanView - 计划视图
- DiffView - 差异对比视图
## 5. package.json 依赖详情
### 根 package.json 主要依赖
生产依赖（部分）：

```
{
  "react": "^19.1.1",
  "react-dom": "^19.1.1",
  "zustand": "^5.0.8",
  "next-themes": "^0.4.6",
  "sonner": "^2.0.7",
  "react-markdown": "^10.1.0",
  "react-syntax-highlighter": "^15.6.6",
  "yaml": "^2.8.1",
  "zod": "^4.3.6",
  // CodeMirror 相关
  "@codemirror/*": "多个包",
  // Radix UI 组件
  "@radix-ui/react-*": "多个包"
}
```
开发依赖（部分）：

```
{
  "typescript": "~5.8.3",
  "vite": "^7.1.2",
  "@vitejs/plugin-react": "^5.0.0",
  "tailwindcss": "^4.0.0",
  "eslint": "^9.33.0",
  "babel-plugin-react-compiler": "^1.0.0"
}
```
### UI 包特有依赖
```
{
  "motion": "^12.23.24",
  "@dnd-kit/core": "^6.3.1",
  "@tanstack/react-virtual": "^3.13.18",
  "fuse.js": "^7.1.0",
  "prismjs": "^1.30.0",
  "qrcode": "^1.5.4"
}
```
## 总结
项目 详情 框架 React 19 + TypeScript 构建工具 Vite 7 样式方案 Tailwind CSS 4 + Radix UI 状态管理 Zustand 项目结构 Monorepo (Bun workspaces) 国际化 无 - 需要添加 主要入口 packages/ui/src/main.tsx 主应用组件 packages/ui/src/App.tsx

如果需要为这个项目添加国际化支持，建议使用 react-i18next 或 formatjs (react-intl) ，因为它们与 React 生态系统集成良好，且 Zustand 可以用于存储当前语言设置。